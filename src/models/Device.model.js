export default class Device {
  constructor(deviceId, deviceName, devicePrice, deviceQuantity) {
    this.deviceId = deviceId;
    this.deviceName = deviceName;
    this.devicePrice = devicePrice;
    this.deviceQuantity = deviceQuantity;
  }
}
